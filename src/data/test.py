import pandas as pd
import re

from periods import Periods

fieldnames = ['section', 'indicator_name', 'indicator_unit', 'indicator_code', 'object_name', 'object level',
              'object_oktmo', 'object_okato', 'year', 'indicator_value', 'comment']

if __name__ == '__main__':
    # data = pd.read_excel('files/Раздел 16 - Транспорт.xlsx')
    # print(data.head())
    # a = 'Республика Крым15)'
    # a = re.sub(r'\d*[)]','', a)
    # print(a)
    # # period = Periods()
    print(float('123'))